name: recordatorios_app
description: Aplicación de recordatorios con linterna y contador de pasos

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^16.3.0
  timezone: ^0.9.1
  shared_preferences: ^2.2.2
  sensors_plus: ^1.3.0
  flashlight_controller: ^0.3.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true